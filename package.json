{
  "name": "rodando-site",
  "version": "1.0.0",
  "private": true,
  "description": "Aprimoramento do site Rodando com SPA em arquivo único e integração Supabase.",
  "scripts": {
    "test": "playwright test",
    "test:live": "node tests/run-live-tests.js",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0"
  }
}
