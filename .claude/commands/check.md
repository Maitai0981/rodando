Verificação completa do projeto: lint, testes unitários e build do frontend.

Execute em sequência e reporte cada etapa:

**1. Lint:**
`cd "C:/Users/mathe/rodando/frontend" && npm run lint 2>&1 | tail -10`

**2. Testes unitários:**
`cd "C:/Users/mathe/rodando/frontend" && npm run test:unit 2>&1 | tail -15`

**3. Build:**
`cd "C:/Users/mathe/rodando/frontend" && npm run build 2>&1 | tail -15`

Para cada etapa mostre: ✓ passou / ✗ falhou com o erro principal. No final, dê um resumo: "X/3 etapas passaram." Se tudo passar, confirme que o projeto está saudável.
