import Typography from '@mui/material/Typography'
import { UiSection } from '../../ui'

export function Categories() {
  return (
    <UiSection>
      <Typography variant="h3">Categories section</Typography>
    </UiSection>
  )
}
