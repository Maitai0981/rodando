[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "on_failure"

# ─── Variáveis obrigatórias no dashboard do Railway ───────────────────────────
# VITE_API_URL=https://<url-do-backend>.railway.app  (build arg — defina ANTES do deploy)
